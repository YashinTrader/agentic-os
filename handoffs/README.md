# Handoffs

One markdown file per agent-to-agent (or agent-to-human) transition on a task.

Naming: `<task-id>__<from-agent>__to__<to-agent>.md`
(append `__<ISO-timestamp>` if there are multiple between the same pair).

See `docs/HANDOFF_PROTOCOL.md` for required sections and rules.
